{% extends 'base.html.twig' %}

{% block body %}

    <div class="cover">
        <div class="coverLinks">
            <span class="coverCtn">
            KIDS AND FAMILY <br>
            CREATEUR D'EXEPERIENCES EN FAMILLE <br>
            <a id="coverBtn" class="btn loginBtn" href="#">Nous Découvrir</a>
            <a id="coverBtn" class="btn loginBtn" href="{{ path('app_login') }}">S'inscrire</a>
                </span>
        </div>
    </div>

{% endblock %}
